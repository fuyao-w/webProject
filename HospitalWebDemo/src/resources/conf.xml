<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--<settings>-->
        <!--<setting name="logImpl" value="STDOUT_LOGGING"/>-->
    <!--</settings>-->
    <plugins>
        <plugin interceptor="com.github.pagehelper.PageInterceptor">
            <!-- config params as the following -->
            <property name="helperDialect" value="mysql"/>
        </plugin>
    </plugins>

    <mappers>
        <mapper resource="doctorMapper"/>
        <mapper resource="policyMapper"/>
        <mapper resource="patientMapper"/>
        <mapper resource="visitMapper"/>
        <mapper resource="userMapper"/>
        <mapper resource="hosinfoMapper"/>
        <mapper resource="drugMapper"/>
        <mapper resource="assessmentMapper"/>
        <mapper resource="blogMapper"/>
        <mapper resource="replyMapper"/>
        <mapper resource="tPlanMapper"/>
    </mappers>





</configuration>